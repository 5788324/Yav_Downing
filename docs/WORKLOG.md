# Yav V2 工作日志

## 2026-07-26：启动 V2 重构

### 完成

- 分析 V1 项目结构和现有抓取链路。
- 确认旧 Tkinter 小工具不再继续扩建，V2 将重做 UI 和代码结构。
- 将项目范围收缩为个人影片磁链图书馆。
- 确认磁链是核心数据，影片资料只用于识别、浏览和筛选。
- 确认多来源按影片名归并并互补资料。
- 确认图书馆支持按厂商、系列和女演员筛选。
- 创建远端 `v1-frozen` 分支。
- 创建远端 `v2-rebuild` 分支。
- 更新 V2 README、开发约束、开发计划和当前状态。

### 决定

- 每部影片只保存影片名、封面、厂商、系列、女演员、日期、时长、收藏和各来源磁链。
- 资料允许缺失，不要求任何单一资源站提供完整数据。
- 普通资料补空不覆盖；女演员合并去重。
- 磁链按 BTIH 去重并追加保存，不覆盖旧磁链。
- SQLite 是唯一业务数据源。
- 不引入用户系统、权限系统、下载器、播放器或复杂任务平台。

## 2026-07-26：完成阶段 0 与阶段 1 最小骨架

- 创建本机 `v2-rebuild` worktree，保留 `main` 的 V1 数据不变。
- 选择 Python 标准库 HTTP 服务 + SQLite：无需新增依赖，适合 Windows 本地运行，后续可直接迁入现有 Python 抓取代码。
- 新建 `backend/db.py` 和 `backend/sources/`；同一影片同 BTIH 只保存一条磁链，并以 `magnet_sources` 保留多个来源。
- 新增本机中文图书馆空页与明确启动命令；服务固定监听 `127.0.0.1`。

## 2026-07-26：V1 到 V2 迁移工具

- 新增 `backend/migrate_v1.py`，要求明确指定旧库和新库路径，默认演练。
- 旧库使用只读连接；迁移仅写入用户指定的新库。封面不复制，仅保留旧库已有的封面网址字段。
- 临时库两次写入核对表明来源页与 BTIH 约束可防止重复。
- 修复收藏状态迁移。
- 审计结果：4,058 个来源页归并为 3,466 部影片；592 部双来源影片与归并减少数量完全一致。
- 日期、厂商、系列和原始标题差异冲突均为 0，可以继续 UI 开发。

## 2026-07-26：完成图书馆 UI 首版

### 界面

- 将原来的单个输入框页面重做为完整中文图书馆界面。
- 建立固定侧栏、数据概览、搜索筛选区、影片内容区和详情弹窗。
- 提供海报墙、收藏架、资料册三种结构视图；保持统一色彩和材质，不采用简单换色主题。
- 影片卡片显示封面、标题、厂商/日期、女演员和各来源磁链数量。
- 无封面时使用内置占位视觉，不依赖外部图片资源。
- 完成桌面和窄屏响应式布局。

### 功能

- 新增影片分页和每页数量选择。
- 新增厂商、系列、女演员、来源和磁链状态组合筛选。
- 新增收藏切换、收藏筛选和快速入口。
- 新增影片详情、手动编辑和手动字段保护。
- 磁链按大小排序；同一 BTIH 的多个来源在同一行显示。
- 新增复制磁链和打开来源网页。
- 支持本地封面路径、远程封面网址和加载失败占位。

### 后端

- 扩展 `GET /api/movies`，支持组合筛选和分页。
- 新增 `GET /api/movies/{id}`、`GET /api/filters` 和本地封面接口。
- 新增 `PATCH /api/movies/{id}` 和收藏接口。
- 列表使用批量查询补充女演员和来源计数，避免逐影片额外查询。
- 增加厂商、系列、收藏、来源和演员关联常用索引。

### 验证

- Python 语法检查通过。
- JavaScript 语法检查通过。
- 临时 SQLite 数据层冒烟测试通过。
- 本地 HTTP 主页、列表和筛选统计接口测试通过。
- 双来源相同 BTIH 的详情展示测试通过。

### 下一步

- 在用户真实迁移数据库上人工查看封面比例、长标题和筛选体验。
- 迁移 JavDB 抓取器。
- 迁移 JPHOO 抓取器。

## 2026-07-26：UI 真实数据复核

- 使用临时迁移库核对首页、分页与 API；收紧布尔筛选参数。


## 2026-07-26 JavDB 扫描骨架

- 新增 JavDB 解析器、扫描器、后台单任务控制、来源配置 API 和来源面板。
- 离线测试覆盖分页、详情、大小、幂等、手动字段、配置与停止状态。


## 2026-07-26 JavDB 临时真实扫描

- 只在 %TEMP%\Yav-javdb-real-test 副本扫描 Wifey；V1 数据与配置未修改。
- 环境超时验证了页码和统计持久化需求，已增加 interrupted 恢复。


- Wifey 后台断点继续完成全系列，实际确认 72 条 JavDB 来源页与磁链追加生效。


## 2026-07-26：开始 JPHOO 来源迁移

实现 `backend.sources.jphoo`、JPHOO 单线程扫描器和最小来源界面。候选磁链不会因弹窗混入的同日作品而盲目归属：匹配当前标题的磁链写入当前影片；带独立番号的其他标题单独保存；无可靠标题的候选只计数不写入。离线测试覆盖系列链接、API 去重、标题分类与重复入库。真实扫描尚未执行，必须使用临时 V2 数据库和专用 JPHOO Edge 资料目录手动登录。

## 2026-07-26：完成 JPHOO 真实验证

在 `C:\tmp\yav-jphoo-real-20260726` 临时 V2 库和独立 Edge profile 上完成 Brazzers Exxtra 受控验证。发现真实 `/prod-api/v2/dht/list` 使用 `infoHash`、`name`、`length` 返回资源，未直接提供 `magnet:`；已最小化扩展解析器，构造标准 BTIH 磁链并保留原始候选标题。单片 12 条候选准确分为当前 6、其他 6；继续扫描第 3 页处理 5 部，新增 60 条磁链，停止和断点续扫均正常。V1 库、配置及 profile 未改动；临时数据和 profile 不提交。

## 2026-07-26：来源管理与数据保护加固

- 保护手动演员资料：一旦用户编辑女演员，后续 JavDB/JPHOO 扫描不会再追加来源演员；未手动编辑时仍正常合并去重。
- `add_magnet` 返回新增 BTIH、来源关联和大小补全结果，扫描统计不再把同一 BTIH 的新来源误报为新增磁链。
- 收紧影片编辑（非空标题、合法日期、http/https 封面网址、0–1440 分钟）和收藏接口（只接受 JSON 布尔值）。
- 来源配置 API 使用白名单；按 `source + id` 删除，运行中系列拒绝删除。来源筛选只列出实际有磁链关联的来源。
- 来源管理从遗留悬浮入口迁到左侧导航，增加焦点样式、弹窗关闭焦点归还、登录/扫描状态轮询和内联编辑。
- 验证使用 `C:\tmp\yav-hardening-api-20260726` 临时 V2 库；未操作 V1 数据库、配置或浏览器资料。临时库、服务和日志均不提交。

## 2026-07-26：验收审计补充

- 新增 5 项回归测试，覆盖手动演员/导航脏资料/磁链保存结果、来源 API 和删除保护、收藏类型、字段校验、实时扫描状态、JPHOO 明确标题与前端静态契约；总计 24 项通过。
- 修复短标题归属漏洞：候选标题只有一个共同短词时不再因为包含关系误归当前影片。
- 删除来源时同时查询持久化扫描状态；即使进程内管理器未持有线程，也会对 `running/stopping` 返回 409。
- 真实受控 JPHOO 复扫在新的临时 V2 副本完成：15 部处理、0 失败、102 条新增唯一 BTIH、174 条新增来源关联，停止后数据库保持一致。

## 2026-07-26：修复 JPHOO 反复登录

- 复核发现旧流程把登录窗口和扫描器拆成独立 Playwright/Edge 上下文；扫描可能使用系列历史 `profile_dir`，而登录窗口关闭后的 `finally` 又无条件写入 `ready`，导致界面反复要求登录或错误显示已登录。
- 新增 `JphooSessionManager`：专用线程串行执行打开、验证、扫描、停止、关闭和存活探测；同一个 persistent context 在会话期间持续复用，扫描器不再自行打开或关闭浏览器。
- Profile 固定为应用数据目录下的 `browser-profile/jphoo`；历史系列 Profile 不删除、不读取，来源面板明确提示其已停用。
- 登录检测只把登录 URL 或可见密码表单认定为 `login_required`；认证存储存在时为 `ready`，其余为 `unknown`。诊断只保存计数和状态，不写入敏感值。
- 新增会话测试，覆盖隐藏密码框、真实系列 origin、统一 Profile、关闭状态和存活探测；全套 27 项测试通过。
- 真实验收使用 `C:\tmp\yav-jphoo-session-fix-20260726` 新临时副本及复制的临时 Profile：打开/验证均为 ready，关闭为 unknown，重启会话后仍为 ready。临时库、Profile 和任何浏览器数据均不提交。

## 2026-07-26：补齐 JPHOO 扫描中退出

- 外部生命周期测试指出：扫描同步运行时，旧 `close()`/`shutdown()` 仅排队命令，可能让 Edge 和 Profile 在 5 秒后仍被占用。
- 修复为自动安全停止：先标记 `stopping` 并通知当前扫描器 stop；扫描返回后同一专用线程立即释放 persistent context。`shutdown()` 等待此线程结束，普通 close 则保留会话线程以便后续重新打开。
- 修复停止与扫描返回之间的状态竞态，已关闭会话不再被数据库中历史 `stopping` 记录覆盖。
- 登录诊断要求受保护认证标记（用户菜单或认证命名存储键），普通网站 Cookie/偏好存储只显示 unknown；不记录键名、Cookie、令牌或值。
- 临时真实 JPHOO 扫描启动后立即 shutdown：scanner 已启动，线程结束、browser/scanner 均释放，最终 closed。30 项自动测试通过。

## 2026-07-26：实现 V2 本地备份

- 新增简单备份命令，使用 SQLite 原生 backup API 避免直接复制正在使用的数据库文件。
- 备份会复制实际存在的本地封面，并在 manifest 中记录缺失封面；不修改原数据、不会下载远程封面，也不覆盖既有备份。
- 补充 Windows SQLite 连接显式关闭，保证备份完成后快照文件不被进程锁定。
- README 同步为“验证登录”流程及备份使用方式。

## 2026-07-26：完成 Windows 打包首版

- 为 V2 新增独立浏览器启动器；默认打开本地界面，`--no-browser` 仅用于验收与排障。
- 新建 PyInstaller spec，收集 `backend/static` 与 Playwright 组件；JPHOO 仍使用本机 Edge，不把浏览器 Profile 打进包内。
- 使用 `dist\Yav-V2.exe --no-browser` 在新临时数据目录启动，首页返回 HTTP 200 且数据库创建成功。

## 2026-07-26：发布候选恢复与迁移入口

- 新增统一启动入口、运行时日志/单实例模块与安全恢复模块。
- V1 扫描改为完整读取后立即关闭只读连接，避免 Windows 文件锁。
- 备份使用同一 SQLite 快照生成封面清单；恢复使用 manifest 校验、预恢复备份、临时复制与数据库切换。
- 新增迁移、恢复、入口测试；38 项自动测试通过。
- 未提交临时数据库、封面、日志、profile、备份或发布 exe。
