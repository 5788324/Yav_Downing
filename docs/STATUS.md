# Yav V2 当前状态

更新时间：2026-07-26

## 当前阶段

阶段 0：冻结旧版并建立 V2 基线。

## 已完成

- 已创建 `v1-frozen` 远端分支，保存当前旧版基线。
- 已创建 `v2-rebuild` 远端分支，作为新版开发分支。
- 已明确项目核心是多来源磁链收集和浏览。
- 已将影片资料范围缩减为影片名、封面、厂商、系列、女演员、日期、时长和收藏。
- 已确定普通资料允许缺失，由多个来源互补。
- 已确定按厂商、系列和女演员筛选浏览。
- 已确定 SQLite 为唯一业务数据源。
- 已更新 README、开发约束和分阶段计划。

## 正在处理

- 建立用户本机的 `v2-rebuild` worktree。
- 备份并统计 V1 数据基线。
- 确定 V2 UI 技术方案和最小数据库结构。

## 下一步

1. 在本机建立独立 V2 worktree。
2. 备份 V1 数据并记录影片、封面和磁链数量。
3. 创建 V2 程序骨架和 SQLite 数据表。

## 已知限制

- 本次执行环境无法直接连接 GitHub 域名，因此无法替用户电脑创建持久化本地 worktree；远端分支和文档提交不受影响。
- V1 的 JavDB/JPHOO 抓取代码尚未迁移到 V2。
- V2 目前还没有可运行界面。
## 2026-07-26 阶段 0 与阶段 1 完成

- 已在 `G:\Antigravity\Yav_Downing-v2` 建立独立 V2 worktree；当前分支为 `v2-rebuild`。
- V1 数据只读统计：4,058 部影片、3,894 条磁链、0 个本地封面文件；未改动任何旧数据。
- 已建立最小 SQLite 表：`movies`、`actresses`、`movie_actresses`、`source_entries`、`magnets`、`magnet_sources` 与 `schema_meta`。
- 已提供影片新增/补空、来源关联、演员去重、BTIH 去重与本机中文空图书馆页面。
- JavDB/JPHOO 目前仅有来源接口占位；V1 解析、扫描和迁移尚未开始。
- 验证：Python 编译（`backend/db.py`、`backend/app.py`）及临时 SQLite 创建、双来源同 BTIH 去重通过。

## 2026-07-26 V1 到 V2 迁移工具

- 新增 `python -m backend.migrate_v1 --old-db <V1库> --new-db <V2库>`；默认 dry-run，只有 `--apply` 才写入新库。
- V1 数据库以 SQLite URI 的只读模式打开；不会复制、修改旧数据库或封面文件。
- dry-run 实测扫描：4,058 影片、920 演员关联、3,894 磁链；真实临时迁移后归并为 3,466 影片、369 演员、4,058 来源页、3,310 BTIH 磁链，无无效磁链。
- V2 影片数低于 V1 是按标准化片名归并多个来源的结果；来源页仍为 4,058 条，重复执行不会增加数量。
