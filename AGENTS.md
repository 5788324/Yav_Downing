# Yav V2 开发约束

## 项目定位

Yav 是个人本地影片磁链图书馆，不是商业产品，也不是通用媒体平台。

项目核心是：按影片名整理来自多个来源的磁链，并提供简单的封面浏览和资料筛选。

## 功能边界

每部影片只管理：

- 影片名
- 封面
- 厂商
- 系列
- 女演员
- 日期
- 时长
- 收藏
- 各来源磁链

当前来源：JavDB、JPHOO。未来来源按相同方式增加。

除非用户明确提出，不增加：

- 用户系统或权限系统
- 下载器或播放器
- 云同步或远程访问
- 推荐、评分、标签系统
- 企业级审批、审计或复杂基础设施
- 复杂插件市场或过度抽象框架

## 数据规则

- SQLite 是唯一业务数据源。
- CSV、Excel、TXT 只用于旧数据迁移或导出，不参与运行状态判断。
- 同一影片可以关联多个来源页面。
- 普通资料只补充缺失值，不随意覆盖已有值。
- 用户手动修改的字段不得被自动扫描覆盖。
- 女演员采用合并去重。
- 磁链按 BTIH 去重，新磁链追加保存，不覆盖旧磁链。
- 每条磁链保留来源、来源网页、大小和发现时间。
- 封面或普通资料抓取失败不得阻止影片和磁链入库。

## 开发原则

- UI 和项目文档使用中文。
- 保持实现直接、可读，优先解决实际需求。
- 不为未来不确定需求提前建设复杂系统。
- 高风险逻辑只包括：旧数据迁移、影片归并、磁链去重和数据库写入；这些部分需要测试。
- 普通 UI 调整不要求编写大量测试。
- 不修改 `v1-frozen` 分支。
- V1 代码只作为抓取逻辑和数据迁移参考。

## 文档更新

完成一轮有实际结果的开发后：

1. 更新 `docs/STATUS.md`；
2. 在 `docs/WORKLOG.md` 追加简短记录；
3. 只有阶段或范围变化时才更新 `docs/PLAN.md`。